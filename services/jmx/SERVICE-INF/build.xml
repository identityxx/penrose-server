<project name="penrose-jmx-service" default="build">

    <description>Penrose JMX Service</description>

    <property file="../../../build.properties"/>

    <tstamp>
        <format property="TIMESTAMP" pattern="yyyyMMddHHmmss"/>
    </tstamp>

    <path id="lib.path">
        <fileset dir="lib">
            <include name="*.jar"/>
        </fileset>
        <fileset dir="../../../lib">
            <include name="**/*.jar"/>
        </fileset>
        <fileset dir="../../../server/lib">
            <include name="*.jar"/>
        </fileset>
        <pathelement location="../../../core/target/classes"/>
        <pathelement location="../../../server/target/classes"/>
    </path>

    <target name="build">
        <mkdir dir="classes"/>
        <javac
            srcdir="src"
            destdir="classes"
            classpathref="lib.path"
            debug="on"
            debuglevel="lines,vars,source"
        />
        <jar jarfile="lib/${product.name}-jmx-service-${product.version}.jar">
            <fileset dir="classes"/>
            <manifest>
              <attribute name="Specification-Version"  value="${product.specification}"/>
              <attribute name="Implementation-Title"   value="${product.title}"/>
              <attribute name="Implementation-Version" value="${product.version}"/>
              <attribute name="Implementation-Vendor"  value="Identyx Corporation"/>
              <attribute name="Build-Date" value="${TIMESTAMP}"/>
            </manifest>
        </jar>
    </target>

    <target name="clean">
        <delete dir="classes" failonerror="false"/>
        <delete file="lib/${product.name}-jmx-service-${product.version}.jar" failonerror="false"/>        
    </target>

</project>