<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sources PUBLIC
  "-//Penrose/DTD Sources ${product.specification}//EN"
  "http://penrose.safehaus.org/dtd/sources.dtd">

<sources>

  <source name="LDAP">
    <partition-name>global</partition-name>
    <connection-name>LDAP</connection-name>
    <parameter>
      <param-name>baseDn</param-name>
      <param-value>${nisSuffix}</param-value>
    </parameter>
  </source>

  <source name="users">
    <partition-name>global</partition-name>
    <connection-name>LDAP</connection-name>
    <field name="dn" primaryKey="true"/>
    <field name="uid"/>
    <field name="seeAlso"/>
    <field name="cn"/>
    <field name="uidNumber"/>
    <field name="gidNumber"/>
    <field name="homeDirectory"/>
    <field name="userPassword"/>
    <field name="loginShell"/>
    <field name="gecos"/>
    <field name="description"/>
    <field name="shadowLastChange"/>
    <field name="shadowMin"/>
    <field name="shadowMax"/>
    <field name="shadowWarning"/>
    <field name="shadowInactive"/>
    <field name="shadowExpire"/>
    <field name="shadowFlag"/>
    <parameter>
      <param-name>objectClasses</param-name>
      <param-value>account,posixAccount,shadowAccount</param-value>
    </parameter>
    <parameter>
      <param-name>baseDn</param-name>
      <param-value>ou=Users,${nisSuffix}</param-value>
    </parameter>
    <parameter>
      <param-name>scope</param-name>
      <param-value>ONELEVEL</param-value>
    </parameter>
  </source>

  <source name="groups">
    <partition-name>global</partition-name>
    <connection-name>LDAP</connection-name>
    <field name="dn" primaryKey="true"/>
    <field name="cn"/>
    <field name="gidNumber"/>
    <field name="userPassword"/>
    <field name="memberUid"/>
    <field name="description"/>
    <parameter>
      <param-name>objectClasses</param-name>
      <param-value>posixGroup</param-value>
    </parameter>
    <parameter>
      <param-name>baseDn</param-name>
      <param-value>ou=Groups,${nisSuffix}</param-value>
    </parameter>
    <parameter>
      <param-name>scope</param-name>
      <param-value>ONELEVEL</param-value>
    </parameter>
  </source>

  <source name="hosts">
    <partition-name>global</partition-name>
    <connection-name>LDAP</connection-name>
    <parameter>
      <param-name>baseDn</param-name>
      <param-value>ou=Hosts,${nisSuffix}</param-value>
    </parameter>
  </source>

  <source name="services">
    <partition-name>global</partition-name>
    <connection-name>LDAP</connection-name>
    <parameter>
      <param-name>baseDn</param-name>
      <param-value>ou=Services,${nisSuffix}</param-value>
    </parameter>
  </source>

  <source name="rpcs">
    <partition-name>global</partition-name>
    <connection-name>LDAP</connection-name>
    <parameter>
      <param-name>baseDn</param-name>
      <param-value>ou=RPCs,${nisSuffix}</param-value>
    </parameter>
  </source>

  <source name="netids">
    <partition-name>global</partition-name>
    <connection-name>LDAP</connection-name>
    <parameter>
      <param-name>baseDn</param-name>
      <param-value>ou=NetIDs,${nisSuffix}</param-value>
    </parameter>
  </source>

  <source name="protocols">
    <partition-name>global</partition-name>
    <connection-name>LDAP</connection-name>
    <parameter>
      <param-name>baseDn</param-name>
      <param-value>ou=Protocols,${nisSuffix}</param-value>
    </parameter>
  </source>

  <source name="aliases">
    <partition-name>global</partition-name>
    <connection-name>LDAP</connection-name>
    <parameter>
      <param-name>baseDn</param-name>
      <param-value>ou=Aliases,${nisSuffix}</param-value>
    </parameter>
  </source>

  <source name="netgroups">
    <partition-name>global</partition-name>
    <connection-name>LDAP</connection-name>
    <parameter>
      <param-name>baseDn</param-name>
      <param-value>ou=Netgroups,${nisSuffix}</param-value>
    </parameter>
  </source>

  <source name="ethers">
    <partition-name>global</partition-name>
    <connection-name>LDAP</connection-name>
    <parameter>
      <param-name>baseDn</param-name>
      <param-value>ou=Ethers,${nisSuffix}</param-value>
    </parameter>
  </source>

  <source name="bootparams">
    <partition-name>global</partition-name>
    <connection-name>LDAP</connection-name>
    <parameter>
      <param-name>baseDn</param-name>
      <param-value>ou=BootParams,${nisSuffix}</param-value>
    </parameter>
  </source>

  <source name="networks">
    <partition-name>global</partition-name>
    <connection-name>LDAP</connection-name>
    <parameter>
      <param-name>baseDn</param-name>
      <param-value>ou=Networks,${nisSuffix}</param-value>
    </parameter>
  </source>

  <source name="automounts">
    <partition-name>global</partition-name>
    <connection-name>LDAP</connection-name>
    <parameter>
      <param-name>baseDn</param-name>
      <param-value>ou=Automounts,${nisSuffix}</param-value>
    </parameter>
  </source>

  <source name="errors">
    <connection-name>Database</connection-name>
    <field name="id" type="INTEGER" primaryKey="true" autoIncrement="true"
        originalType="INTEGER generated by default as identity"/>
    <field name="time" type="DATETIME"/>
    <field name="title" type="TEXT" originalType="VARCHAR"/>
    <field name="description" type="TEXT" originalType="VARCHAR"/>
    <parameter>
      <param-name>table</param-name>
      <param-value>errors</param-value>
    </parameter>
    <parameter>
      <param-name>create</param-name>
      <param-value>true</param-value>
    </parameter>
  </source>

</sources>